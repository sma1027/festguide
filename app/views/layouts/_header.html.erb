<!-- Static navbar -->
<nav class="navbar navbar-default navbar-static-top">
  <div class="container">
    <div class="navbar-header">
      <%= link_to '', root_path, :class => "navbar-brand fa fa-home fa-2x"%>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <li><a href="#" class="pull-left">The Fest Project</a></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><strong><%= current_user.email %></strong> <span class="caret"></span></a>
              <ul class="dropdown-menu" role="menu">
                <li><%= link_to 'Edit profile', edit_user_registration_path %></li>
                <li class="divider"></li>
                <li><%= link_to "Logout", destroy_user_session_path, method: :delete%></li>
                <li class="divider"></li>
                <li class="dropdown-header">admin: <strong><%= current_user.admin? %></li>
              </ul>
            </li>
        <% else %>
          <li><%= link_to "Login", new_user_session_path, :class => 'pull-right' %></li>
        <% end %>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</nav>